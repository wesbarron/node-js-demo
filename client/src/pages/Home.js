import React, {Component} from 'react';

class Home extends Component{

    //write the function to do fetch

    render(){
        return (
            <h1>This is a dynamic web page via node and react</h1>
        );
    }
}

export default Home;